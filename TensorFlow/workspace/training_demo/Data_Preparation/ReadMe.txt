Run the Create_Dataset script.
	1) it will generate images in Dataset folder
	2) split those images into train and test images into two folders
	3) Create_Dataset Script also generates lables.csv file
	4) Seperate the labels into train_labels & eval_labels in training_data folder.

Run the generate_tfrecord script
	1) it will generate tfrecord files for both train_labels and eval_labels.


Copy the train images ,test images into images folder in training_demo folder
Copy the tfrecord files into annotations folder


	